/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', "raw.githubusercontent.com", "github.com", "gs://washmecarwashlongview-27e04.appspot.com", ""],
      }
};

export default nextConfig;
